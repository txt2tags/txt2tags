%!target: html
%!options: --no-headers

% Convert comments to titles
%!preproc : '^%%% ([^ \t].*)'			'== \1 =='
% Trick to ensure the body init (don't loose comments)
%!preproc : '^BODYINIT$'		''

% vim: ft=txt2tags
